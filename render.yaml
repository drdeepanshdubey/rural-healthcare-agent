services:
  - type: web
    name: rural-healthcare-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 8080
      - key: GEMINI_API_KEY
        sync: false
